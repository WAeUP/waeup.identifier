Ubuntu-12.04 (precise) box
==========================

This (outdated) image is needed to test travis-ci runs.

Build the image
---------------

The image is built like so::

  $ docker build -t kivy-precise .

